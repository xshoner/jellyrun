# 모바일 렌더링 성능 수정

## 확인한 병목
- 일반 배경은 매 프레임 88회의 drawImage 호출을 수행했다. 화면 밖 타일과 28개 조각으로 구성된 경계 혼합도 매번 다시 그렸다.
- requestAnimationFrame마다 그리기와 HUD DOM 갱신을 수행하여 120Hz 화면에서는 둘 다 초당 120회 실행됐다.
- 일시정지와 게임 종료 상태에서도 전체 장면을 계속 다시 그렸다.
- 화면 밖 젤리와 일반 장애물도 렌더링했다.

## 변경
- 배경 경계 140×720 영역을 이미지 로딩 후 한 번 캐시한다. 전체 배경 복제 대신 작은 경계만 저장하며 추가 RGBA 저장량은 약 3.85 MiB다.
- 화면과 겹치는 배경만 그린다. 경계 혼합과 배경 순서를 유지한다.
- 실제 게임 물리 계산은 기존 120Hz 고정 간격 그대로 유지한다. 화면 출력만 최대 60Hz, 로비는 30Hz로 제한한다.
- HUD 갱신은 100ms 간격으로 수행하며 모드 변경은 즉시 반영한다.
- 정지된 화면은 모드가 바뀔 때 그리며, 숨겨진 탭에서는 그리기와 HUD 갱신을 생략한다.
- 화면 밖 젤리와 일반 장애물의 그리기를 생략한다. 충돌 계산과 객체 수명에는 영향을 주지 않는다.
- index.html 및 서비스 워커 캐시 버전을 v21로 올렸다.

## 검증
실행: `node test-game.cjs`, `node test-events.cjs`, `node test-audio.cjs`, `node test-performance.cjs`.

- 기존 게임 83개, 이벤트/생명주기 44개, 오디오 9개 검사 통과.
- 추가 검사: 배경 전체 경로에서 프레임당 최대 3회 그리기, 60/90/120/144Hz 입력에서 2초간 120회 렌더링 및 동일한 물리 시간, 일시정지/숨김/화면 밖 객체 처리.
- 이벤트 검사 중 모든 젤리 종류의 그림을 확인하는 테스트는 생성 위치(화면 밖) 대신 화면 안으로 배치하도록 수정했다.
- 로컬 HTTP 서버와 실제 Chrome headless에서 모바일 viewport 844×390, DPR 3, 터치 설정으로 로딩·시작·점프·보호막·스피드 실행 및 스크린샷 확인. JavaScript 실행 오류 없음.
- 동일 배경 스크롤 300프레임의 drawImage 호출: 기존 26,400회 → 수정 774회(97.1% 감소).
- 위 반복문의 단일 실행 JavaScript 경과 시간: 19.7ms → 6.6ms. GPU 완료 시간이나 실제 휴대폰 FPS 측정이 아니므로 단말 FPS 개선율로 해석하지 않는다.

## 적용 범위와 남은 확인
현재 수정은 로컬 소스에 적용됐다. GitHub 업로드와 Vercel 배포는 수행하지 않았다.
실제 문제가 발생한 휴대폰 모델/브라우저 정보가 없어 해당 기기의 랙 원인을 확정하지는 못했다. 배포 후 해당 단말에서 일반 달리기, 레이저/폭탄, 보너스 스테이지, 5분 이상 연속 플레이를 확인해야 한다.
원본 PNG 전체의 이론상 RGBA 저장량은 약 162 MiB로, 저메모리 단말에서는 이미지 메모리 및 효과의 shadowBlur/filter도 추가 측정 대상이다. 이번 수정은 이미지 품질과 효과를 유지하면서 확인 가능한 반복 렌더링 병목을 제거한다.
